import * as React from 'react'
import docMd from '../../docs/confirm.md'
import Markdown from '../../markdown'

const Confirm = () => {
  return <div><Markdown input={docMd} /></div>
};

export default Confirm
