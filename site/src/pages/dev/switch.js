import * as React from 'react'
import docMd from '../../docs/switch.md'
import Markdown from '../../markdown'

const Switch = () => (
  <div>
    <Markdown input={docMd} />
  </div>
)

export default Switch
