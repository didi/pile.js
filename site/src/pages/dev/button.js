import * as React from 'react'
import buttonMd from '../../docs/button.md'
import Markdown from '../../markdown'

const Button = () => (
  <div>
    <Markdown input={buttonMd} />
  </div>
)

export default Button
